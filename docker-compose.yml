version: "3"
services:
  node:
    build: .
    network_mode: "host"
    privileged: true
  influxdb:
    image: "influxdb"
    ports:
      - "8086:8086"
    volumes:
      - "influxdb:/var/lib/influxdb"

volumes:
  influxdb: